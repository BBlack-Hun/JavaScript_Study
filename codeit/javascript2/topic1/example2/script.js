// id 태그 선택하기
const myTag = document.getElementById('myNumber');
console.log(myTag);

const myBtn = document.getElementById('btn');
console.log(myBtn);

/**
 * 존재하지 않는 태그를 선택하면, undefined가 아닌 null값이 출력된다
 */
